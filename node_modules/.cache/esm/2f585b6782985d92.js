let inquirer;_829‍.w("inquirer",[["default",["inquirer"],function(v){inquirer=v}]]);const { stdout } = require('process');
const chalk = require('chalk');

const fs = require('fs');

async function config() {
    var config = await inquirer.prompt({
        name: 'username',
        type: 'input',
        prefix: '>',
        message: 'Username:'
    });
};

module.exports = config;